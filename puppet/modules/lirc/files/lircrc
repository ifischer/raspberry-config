 begin
     prog   = irexec
     button = KEY_PLAY
     config = /usr/local/bin/mopidy-cli radio
 end

 begin
     prog   = irexec
     button = KEY_STOP
     config = /usr/local/bin/mopidy-cli stop
 end

 begin
     prog   = irexec
     button = KEY_PAUSE
     config = /usr/local/bin/mopidy-cli pause
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_1
     config = /usr/local/bin/mopidy-cli play --tlid 1
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_1
     config = /usr/local/bin/mopidy-cli play --tlid 1
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_2
     config = /usr/local/bin/mopidy-cli play --tlid 2
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_3
     config = /usr/local/bin/mopidy-cli play --tlid 3
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_4
     config = /usr/local/bin/mopidy-cli play --tlid 4
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_5
     config = /usr/local/bin/mopidy-cli play --tlid 5
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_6
     config = /usr/local/bin/mopidy-cli play --tlid 6
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_7
     config = /usr/local/bin/mopidy-cli play --tlid 7
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_8
     config = /usr/local/bin/mopidy-cli play --tlid 8
 end

 begin
     prog   = irexec
     button = KEY_NUMERIC_9
     config = /usr/local/bin/mopidy-cli play --tlid 9
 end